---
slug: gatsby-redirect
---

# How to Redirect Gatsby URLs

Use `js÷actions.createRedirect()` in `gatsby-node.js` like so

```js
exports.createPages = ({ actions }) => {
  actions.createRedirect({
		fromPath: `/`,
		toPath: `/fr`,
		isPermanent: true,
	})
}
```

## Further Reading

- [createRedirect, Actions, Gatsbyjs.org](https://www.gatsbyjs.org/docs/actions/#createRedirect)
